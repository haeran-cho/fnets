#'  Simulated example common+idiosyncratic time series
#'
#'  Example `p=100`, `n=200` time series, where the common component is generated using Model 1 and the idiosyncratic component is generated with a sparse VAR.
#'
"sample.data"

## generate example data
# set.seed(222)
# n <- 200
# p<- 100
# chi <- sim.factor.M1(n,p)
# xi <- sim.idio(n,p)
# sample.data <- chi$data + xi$data
